/**
 * Codec contracts and encoded payload values used by the storage stack.
 *
 * <p>Codecs translate domain objects to bytes before values reach layers and
 * reconstruct domain objects after envelopes are loaded.</p>
 */
package io.github.silentdevelopment.strata.codec;
