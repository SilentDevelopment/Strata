/**
 * Stored envelope and decoded entry model types.
 *
 * <p>Layer implementations operate on envelopes. Stack consumers receive
 * decoded entries that preserve stamps and metadata for concurrency and
 * indexing decisions.</p>
 */
package io.github.silentdevelopment.strata.entry;
