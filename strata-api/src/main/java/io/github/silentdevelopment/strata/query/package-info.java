/**
 * Query model types for capability-dependent indexed lookup.
 *
 * <p>Query support depends on the configured layers. Some backends cannot safely
 * enumerate or index entries and report unsupported query operations.</p>
 */
package io.github.silentdevelopment.strata.query;
