/**
 * Result, status, and failure values returned by stack and layer operations.
 *
 * <p>Expected storage failures are represented as result values. Programming
 * errors and invalid configuration may still be thrown by API methods.</p>
 */
package io.github.silentdevelopment.strata.result;
