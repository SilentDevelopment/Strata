/**
 * Redisson lock manager implementation.
 *
 * <p>Redisson locks are thread-bound; returned handles must be closed by the
 * owning thread.</p>
 */
package io.github.silentdevelopment.strata.lock.redis.redisson;
