/**
 * In-memory soft layer implementation.
 *
 * <p>The memory layer provides local volatile key-value storage with TTL and
 * conditional-write support for non-authoritative stack acceleration.</p>
 */
package io.github.silentdevelopment.strata.soft.memory;
